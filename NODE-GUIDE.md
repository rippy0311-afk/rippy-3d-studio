# ノードでゲームを作る

「◇ ノード」を開き、イベントの出力から処理の入力へ白い線をつなぎます。「▶ プレイ」で実行し、Esc または「停止」で編集時の位置・色に戻ります。

## イベント

- 開始、毎フレーム、形が作られたとき
- キーを押したとき・離したとき・押している間・文字入力。数字・英字・記号・Tab を選択可能
- クリック、ダブルクリック、押下・解放・長押し、マウスの出入り・移動・ホバー、ホイール、タッチ、ドラッグ開始・移動・終了
- 形が触れたとき・触れている間・離れたとき・離れている間
- 指定時間後、一定間隔、条件が成立したとき・成立している間
- UI の入力・変更・送信・フォーカス・キー・マウス操作
- 質問に回答したとき、すべての質問に回答したとき
- 変数が変わったとき、自作イベントを受信したとき

ポインターの対象は「画面全体」「すべての形」「特定の形」から選択できます。処理の対象を「イベントの形」「相手の形」「作成した形」にすると、同じ処理を複数の形に使えます。

## 値と処理を組み合わせる

数値やテキストの入力欄では、固定値のほかに変数・イベントの値・形の XYZ 座標を選べます。クリック位置、押したキー、回答、相手の形などを次の処理に渡せます。

変数、四則演算、余り、乱数、文字列の結合、比較、条件分岐、回数指定の繰り返し、待機、関数、自作イベントを組み合わせられます。関数は名前を合わせて呼び出します。繰り返しの「くり返す」出力には本体を、「次へ」には終了後の処理を接続します。

「画面UIを作る」でボタン・入力欄・チェックボックス・スライダー・選択欄・フォームを作り、同じ UI 名のイベントで操作を受け取れます。「質問する」の回答は指定した名前の変数にも入ります。UI の文字入力中はゲーム全体のキー操作を止めます。Esc はゲーム停止用です。

サンプルには「キーで移動」「クリックした形を着色」「タイマーと変数」「質問と回答」「関数と繰り返し」があります。置き換えは Ctrl+Z で戻せます。ゲームの保存には形とノードが含まれます。

## 判定と対応範囲

接触は形を囲む箱の重なりで判定します。回転による箱の更新は次のフレームに反映されます。物理衝突で自動的に押し返す機能ではありません。

Game Lab・App Lab・Sprite Lab の入力／ゲーム用イベントを、この 3D スタジオ向けにまとめています。専用機器、カメラ・マイク、外部通信やクラウドサービスが必要な機能は対象外です。Code.org のプロジェクトファイルを直接読み込む機能はありません。


## 最初の定義とプレイ操作

「初期設定 → 最初の定義」を1個配置します。線をつながなくてもプレイ開始時に設定が適用されます。「次へ」には開始時の処理をつなげます。

- プレイヤー：カメラが追う形を1個選択。「なし」なら編集時の注視点を中心に視点を回せます。
- 追加の操作対象：プレイヤーとは別に複数チェックできます。同じ移動キーでまとめて動きます。
- 視点：三人称（後方／前方）または一人称。前方向は形のローカル −Z／＋Z／＋X／−X から選び、開始時の視点を決めます。
- 移動キー、速度、カメラ距離、注視点の高さ、目の高さ、視点感度、移動方向へプレイヤーを向ける設定を変更できます。

プレイ中は右ドラッグで慣性なしの視点回転、ホイールでズームできます。一人称では画角を変更し、プレイヤー自身の形はカメラに映しません。WASD／矢印キーはカメラの方向を基準に地面と平行に移動します。追加の操作対象も同じ距離だけ移動します。重力や壁による移動制限はありません。既存の移動ノードと併用すると移動量が加算されるため、ノードだけで操作する場合は移動キーを「なし」にしてください。

停止すると位置・向き・視点・ズームが編集時に戻ります。「プレイヤーとカメラの定義」サンプルでも試せます。


## 1つの出力から複数へ接続

同じ出力の○を再び選び、別の入力へ接続すると線を追加できます。処理は接続した順に実行します。「待つ」があれば、その待機と後続処理を終えてから次の接続先へ進みます。if の true／else や繰り返しの本体でも使えます。線をクリックするとその1本だけ外せます。
